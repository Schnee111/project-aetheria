import type { ChapterData } from '../../types';

import { scenes } from './scenes';

export const chapter1: ChapterData = {
  id: 'CH1',
  title: 'The Reincarnated Architect',
  scenes,
};

export { scenes } from './scenes';
